name: Validate queue

on:
  merge_group:

jobs:

  validate-queue:
    if: ${{ github.event_name == 'merge_group' }}
    runs-on: ubuntu-slim
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Display info
      run: | 
        echo "merge queue check!!"
        pwd
        tree -a -I '.git'
        git status
    - name: Run fast CI (emulated by a short sleep)
      run: sleep 30
