name: Validate pull request before putting into queue

on:
  pull_request:
  merge_group:

jobs:

  validate-pr:
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-slim
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Display info
      run: | 
        echo "pull request!"
        pwd
        tree -a -I '.git'
        git status
    - name: Run fast CI (emulated by a short sleep)
      run: sleep 5

  dummy-merge-queue:
    if: ${{ github.event_name == 'merge_group' }}
    runs-on: ubuntu-slim
    steps:
    - name: Display info
      run: | 
        echo "This doesn't normally run!"
    - name: How did we get here?
      run: exit 1

